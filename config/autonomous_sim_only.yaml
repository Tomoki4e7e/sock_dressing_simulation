extends: autonomous_comparison_base.yaml

inference:
  training_data_root: .
  training_manifest: artifacts/autonomous-comparison/manifests/sim_only.yaml
  training_dataset_name: autonomous_sim_only
  output_dir: artifacts/autonomous-comparison/models/sim_only
  checkpoint: artifacts/autonomous-comparison/models/sim_only/SARNN_latest.pth
  stats: artifacts/autonomous-comparison/models/sim_only/data.json
